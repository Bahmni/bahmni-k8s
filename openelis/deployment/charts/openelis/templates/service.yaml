apiVersion: v1
kind: Service
metadata:
  name: {{ include "openelis.fullname" . }}
  labels:
    {{- include "openelis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openelis.selectorLabels" . | nindent 4 }}
