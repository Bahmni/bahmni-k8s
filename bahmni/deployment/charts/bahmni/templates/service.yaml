apiVersion: v1
kind: Service
metadata:
  name: {{ include "bahmni.fullname" . }}
  labels:
    {{- include "bahmni.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bahmni.selectorLabels" . | nindent 4 }}
